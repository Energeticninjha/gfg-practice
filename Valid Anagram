class Solution {
    public boolean isAnagram(String s, String t) {
        s = s.toCharArray();
        t = t.toCharArray();
Arrays.sort(s);
Arrays.sort(t);
for(int i=0;i<s.length();i++){
    if(s.charAt(i) != t.charA(i)){
        return false;
    }
}
return true;
    }
}
